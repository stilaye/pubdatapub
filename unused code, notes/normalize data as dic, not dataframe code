normalize data as dic, not dataframe code
		#due to struct, dictionaries and lists are where just values should be. Below changes that.
		kc=df_ob.country[i].keys()
		#justcname=df_ob.country[0].pop(kc[0])
		justcname=df_ob.country[0].keys()[0]
		df_ob.country.replace(df_ob.country,justcname,inplace=True)
		ki=df_ob.indicator[0].keys()
		justiname=df_ob.indicator[0].keys()[0]
		df_ob.indicator.replace(df_ob.indicator,justiname,inplace=True)
		final_df=final_df.append(df_ob)